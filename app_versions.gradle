ext{
    //是否开启单模块调试
    isDebug=false

    //sdk版本
    app_sdk = [
            compileSdk: 33,
            minSdk: 24,
            targetSdk: 33,
    ]

    //三方库版本
    versions =[
            arouter_version : "1.5.2",
    ]
}